<div class="contents">
  <h1>Editing Book</h1>
  <% if @book.errors.any? %>
    <div class= <%= "errors" if @book.errors.any? %>>
      <p class= <%= "error_message" if @book.errors.any? %>>
        <%= @book.errors.count %>errors prohibited this book form beong saved:
      </p>
        <% @book.errors.full_messages.each do |message| %>
          <p>・<%= message %></p>
        <% end %>
    </div>
  <% end %>
  <%= form_with model: @book, url: book_path(@book.id), method: :patch do |f| %>
    <div>
      <p class="form_text">Title</p>
      <%= f.text_field :title %>
    </div>
    <div>
      <p class="form_text">Body</p>
      <%= f.text_area :body %>
    </div>
    <div class="text button">
      <%= f.submit 'Update Book' %>
    </div>
    <p class="text"><%= link_to 'Show', book_path(@book.id) %> | <%= link_to 'Back', '/books' %></p>
  <% end %>
</div>